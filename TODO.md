## Backlogs

### TODO FEAT:SEO

- [ ] Add description frontmatter to each project's `index.md` (in source repos) — or use the GitHub description via API
- [ ] Add a `<meta name="description">` tag in the head of the generated docs (from GitHub API)
- [ ] Add a `<title>` meta tag in the head of the generated docs (from GitHub repo name via API)

### FIXME Generated Docs

- [ ] For all repos: hide the code structure in the generated docs
- [ ] Use the GitHub API to generate SEO info (title, description, slug…)

---

## In Progress
